% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_validation.R
\name{validate_prep_inputs}
\alias{validate_prep_inputs}
\title{Validate inputs for the field genotyping prep app}
\usage{
validate_prep_inputs(folder, tsv, vcf_path)
}
\arguments{
\item{folder}{string. Path to the directory containing the BAM files. Required.}

\item{tsv}{data.frame. Parsed metadata TSV with columns \code{bam_file},
\code{individual_id}, \code{group}, \code{color}. Required.}

\item{vcf_path}{string. Path to the VCF file. Required.}
}
\value{
A list with elements \code{ok} (logical), \code{errors}
  (character vector of blocking problems), \code{warnings} (character
  vector of non-blocking notices), and \code{passes} (character vector
  of human-readable confirmations).
}
\description{
Runs the upfront batch validation described in the field-prep-app spec:
TSV schema rules, BAM existence, BAM integrity (\code{samtools
quickcheck}), and VCF/BAM chromosome-name agreement. Pure function, no
Shiny dependency.
}
