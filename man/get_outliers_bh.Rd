% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_genetics_pcadapt.R
\name{get_outliers_bh}
\alias{get_outliers_bh}
\title{Apply Benjamini-Hochberg correction, reduced to one row per locus.}
\usage{
get_outliers_bh(pcadapt_result, locus_names, alpha)
}
\arguments{
\item{pcadapt_result}{a pcadapt result object, as returned by run_pcadapt_scan().}

\item{locus_names}{character vector, one entry per pcadapt_result$pvalues entry.}

\item{alpha}{numeric. BH significance threshold.}
}
\value{
data.frame with columns locus, pvalue, padj, outlier.
}
\description{
`locus_names` has one entry per pcadapt_result$pvalues entry (i.e. per
one-hot column); loci sharing multiple haplotype columns are reduced to
their minimum pvalue before BH correction. pcadapt sets a column's
pvalue to NA when it fails the `min.maf` filter (common for rare
haplotype-column variants), so the minimum is taken over the non-NA
pvalues within a locus; a locus is only NA overall if every one of its
columns was filtered.
}
